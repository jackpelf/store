#include<stdio.h>

int main()
{
	int sum = 0;
	int n;
	while(EOF != scanf("%d", &n))
	{
		sum = 0;
		while(n)
		{
			sum += n-n%3;
			n = n/3 + n%3;
			if(n<3)
			{
				sum += n;
				break;
			}
		}
		printf("%d\n", sum);
	}
}
